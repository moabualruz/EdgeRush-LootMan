package com.edgerush.datasync.domain.integrations.model

/**
 * Represents an external data source that can be synchronized
 */
enum class ExternalDataSource {
    WOWAUDIT,
    WARCRAFT_LOGS,
    RAIDBOTS
}
